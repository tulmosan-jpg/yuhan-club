# 유한대 식품영양학과 — Style Reference
> 학과 동아리 활동(멘토링 보고서·출석·대외활동·자격증·리워드)을 관리하는 앱. 단정함, 차분함, 신뢰감.

**Theme:** light
**Platform:** mobile 390×844

> 값은 기존 코드(`lib/app/theme.dart`, `lib/widgets/app_dialog.dart`)에서 추출했다.

## Colors

| 이름 | 값 | 쓰는 곳 |
|---|---|---|
| 바탕 | #FAFAFA | 화면 배경 (`AppTheme.scaffoldBg`) |
| 표면 | #FFFFFF | 카드, 다이얼로그, 바텀시트 |
| 글자 기본 | #18181B | 제목, 본문 |
| 글자 보조 | #6B7280 | 날짜, 이메일 같은 부가 정보 |
| 테두리 | #E4E4E7 | 카드 테두리, 구분선 |
| 포인트 색 | #1F9D6B | 주 버튼, 선택된 상태 (`brand500`) |
| 포인트 색 옅은 것 | #E0F5EB | 선택된 칩·아바타 배경 (`brandTonal`) |

바탕과 표면 중에 **표면**이 더 밝다.
포인트 색은 하나만 쓴다. 위 표에 없는 색은 추가하지 않는다.

예외로 **경고/삭제에만** #E53E3E 를 쓴다. 강조가 아니라 위험 표시 전용이다.

## Typography

- Font: Pretendard (fallback: system-ui, sans-serif)
- 화면 제목 26px / 굵기 700
- 섹션 제목 19px / 굵기 700
- 카드 제목 16px / 굵기 700
- 본문 15px / 굵기 400
- 부가 정보 12.5px / 굵기 600
- 굵기는 **700**과 **400**만 쓴다. 보조 라벨에 한해 600을 허용한다
- 위 다섯 종류 말고 다른 글자 크기는 쓰지 않는다

## Spacing

- 간격은 4·8·12·16·24·32만 쓴다. 그 사이 값은 쓰지 않는다
- 화면 좌우 여백 24px (목록 화면은 16px)
- 카드 안쪽 여백 16px
- 카드와 카드 사이 8px
- 10px, 18px 같은 중간값 금지

> 기존 화면 일부에 10·14·18·22 가 남아 있다. 새로 만드는 화면에는 쓰지 않고,
> 해당 화면을 손볼 때 위 스케일로 정리한다.

## Radius

- 버튼 14px (`radiusField`)
- 카드 24px (`radiusCard`)
- 입력창 14px
- 원형은 999 (`radiusPill`)
- 위 값 말고 다른 모서리 값은 쓰지 않는다

## Elevation

- 깊이는 **테두리**로만 표현한다
- 그림자를 쓰지 않는다. 카드는 #E4E4E7 테두리 + 흰 배경으로 구분한다
  (`cardTheme.elevation = 0`)
- 화면마다 다른 방식을 섞지 않는다

## Components

- 주 버튼 — #1F9D6B 배경 + 흰 글자, 높이 52, radius 14, 화면당 하나만
- 보조 버튼 — 흰 배경 + #E4E4E7 테두리 + #52525B 글자, 높이 52, radius 14
- 카드 — 흰 배경 + #E4E4E7 테두리, radius 24, 안쪽 여백 16
- 입력창 — #F1F2F4 채움, 테두리 없음, 포커스 시 #1F9D6B 1.6px, radius 14
- 칩(필터) — 기본 #E0F5EB 배경, 선택되면 #1F9D6B 배경 + 흰 글자, radius 999
- 다이얼로그 — 흰 카드 radius 24, 아이콘 배지(포인트색 12% 원형 56px),
  하단에 취소/액션 **나란히**. `lib/widgets/app_dialog.dart` 공용 함수만 쓴다

## Guidelines

**DO**
- 포인트 색은 **주 버튼과 선택된 상태**에만 쓴다
- 화면마다 주 버튼은 하나. 나머지는 보조 버튼이나 글자 링크
- 팝업은 반드시 `app_dialog.dart` 의 공용 함수를 쓴다. 기본 `AlertDialog` 금지
- 모션은 짧고 한 방향으로. 진입 애니메이션은 600ms 이내
- 접근성 설정에서 모션 줄이기가 켜져 있으면 애니메이션을 건너뛴다

**DON'T**
- 그라데이션, 글로우 금지
- 그림자로 깊이를 만들지 않는다 (테두리로 통일)
- 빨강(#E53E3E)을 강조색으로 쓰지 않는다. 삭제·경고 전용
- 정해둔 것 말고 다른 색 추가 금지
